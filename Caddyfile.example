# Replace YOUR_DOMAIN with your actual domain (e.g., webring.example.com)
YOUR_DOMAIN {
    # API requests go to backend
    handle /api/* {
        reverse_proxy backend:3000
    }

    # Everything else goes to frontend
    handle {
        reverse_proxy frontend:80
    }
}
