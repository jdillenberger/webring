{
  "name": "webring-backend",
  "version": "1.0.0",
  "description": "Webring API backend",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "hash-password": "node -e \"import('bcrypt').then(b => b.default.hash(process.argv[1], 10).then(console.log))\" --"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.2",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^9.0.1"
  }
}
